package com.createorder.createorder.model;

public enum OrderStatus {
    SUCCESS,
    FAILED,
    INSUFFICIENT_FUND,
    TIMEOUT,
    UNKNOWN,
    CANCLED,
    IN_PROGRESS,
    FORCED,
    OUT_OF_STOCK,
    ITEM_NOT_FOUND
}
